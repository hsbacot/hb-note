<h1><%= @user.username %>'s Blog</h1>
<% if @user.banned != true %>
  <% @notes.each do |note| %>
    <h3><%= link_to note.title, "#" %></h3>
  <% end %>
<% else %>
  <p>User is banned from blogging</p>
<% end %>