<h1>Moderate <%= @user.username %></h1>
<% if @posts.empty? %>
  <h3>The User has no public posts</h3>
<% else %>
    <table class="table table-striped">
      <thead>
        <tr>
          <th>Post Title</th>
          <th>Allowed to publish?</th>
          <th>Update</th>
        </tr>
      </thead>
      <tbody>
        <% @posts.each do |post| %>
        <tr>
          <td><%= post.title %> post id => <%= post.id %></td>
          <td><%= post.publishable? %></td>
          <td><%= button_to "Ban", admin_ban_path(post), :class => "btn btn-info", :method => :patch %></td>
        </tr>
        <% end %>
      </tbody>
    </table>
<% end %>